% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_registry_entry.R
\name{new_registry_entry}
\alias{new_registry_entry}
\title{Construct a registry entry}
\usage{
new_registry_entry(
  id,
  body = NULL,
  body_manufacturer = NULL,
  body_serial = NULL,
  device = NULL,
  device_manufacturer = NULL,
  device_serial = NULL,
  lens = NULL,
  lens_manufacturer = NULL,
  lens_serial = NULL,
  auxiliary_lens = NULL,
  auxiliary_lens_manufacturer = NULL,
  auxiliary_lens_serial = NULL,
  institution = NULL
)
}
\arguments{
\item{id}{character vector of length one. Unique identifier of the camera
system within a project, institution, or personal workflow.}

\item{body}{optional character vector of length one. Camera body model.}

\item{body_manufacturer}{optional character vector of length one.}

\item{body_serial}{optional character vector of length one. Serial number as
physically printed on the body.}

\item{device}{optional character vector of length one. Image acquisition
device used with an auxiliary lens (e.g. smartphone model).}

\item{device_manufacturer}{optional character vector of length one.}

\item{device_serial}{optional character vector of length one. Serial number as
physically printed on the device.}

\item{lens}{optional character vector of length one. Primary lens model.}

\item{lens_manufacturer}{optional character vector of length one.}

\item{lens_serial}{optional character vector of length one. Serial number as
physically printed on the lens.}

\item{auxiliary_lens}{optional character vector of length one. Auxiliary
fisheye lens.}

\item{auxiliary_lens_manufacturer}{optional character vector of length one.}

\item{auxiliary_lens_serial}{optional character vector of length one.}

\item{institution}{optional character vector of length one. Institution where
the instrument is usually stored.}
}
\value{
An object of class \link{hs_registry_entry}.
}
\description{
Construct a registry entry describing a imaging system and its
physical components (camera body, device, lens, and/or auxiliary lens).
}
\details{
A registry entry represents a \emph{physical acquisition system}
rather than a single device. Depending on the configuration, the system may
correspond to typical acquisition setups such as:

\itemize{
\item a reflex or mirrorless camera body with an interchangeable lens,
\item a compact camera (camera body with a fixed lens) with an auxiliary lens,
\item an image acquisition device with or without an auxiliary hemispherical lens.
}

For this reason, information related to camera bodies, devices, lenses, and
auxiliary lenses is stored internally as separate components, each with its
own identity (model, manufacturer, serial number).
}
\examples{
foo <- new_registry_entry(
  "Nikon_Coolpix5700.FCE9.CIEFAP",
  body = "E5700",
  body_manufacturer = "NIKON CORP",
  body_serial = "7053067",
  lens = "Zoom Nikkor ED 8.9-71.2mm 1:2.8-4.2",
  lens_manufacturer = "NIKON CORP",
  auxiliary_lens = "Fisheye Converter FC-E9 0.2x",
  auxiliary_lens_manufacturer = "NIKON CORP",
  institution = "CIEFAP"
)

foo
summary(foo)
}
\seealso{
\code{\link[=add_file_sig]{add_file_sig()}}, \code{\link[=add_embedded_metadata_sig]{add_embedded_metadata_sig()}}, \code{\link[=add_geometry_spec]{add_geometry_spec()}},
\code{\link[=add_radiometry_spec]{add_radiometry_spec()}}
}
